# Operations

The local profile writes `state.json` using temporary-file replacement. Back up the data directory only while the service is stopped or after a filesystem snapshot. An invalid SBOM is rejected before graph mutation; quarantine it with its request id and digest. Advisory feeds are replayed by id and must be monotonically audited by the caller. Restore is verified by re-opening the service and querying product risk plus graph paths.
